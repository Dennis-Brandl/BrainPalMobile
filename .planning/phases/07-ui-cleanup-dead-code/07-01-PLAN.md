---
phase: 07-ui-cleanup-dead-code
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/mobile/app/(tabs)/execute.tsx
  - apps/mobile/app/(tabs)/_layout.tsx
  - apps/mobile/app/(tabs)/overview.tsx
  - apps/mobile/src/components/form/FormActionButtons.tsx
  - apps/mobile/src/components/form/index.ts
  - apps/mobile/src/components/carousel/StepCarousel.tsx
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Execute tab with zero active workflows shows empty state with guidance text directing user to Library tab"
    - "Execute tab with exactly one active workflow auto-navigates to /execution/[instanceId]"
    - "Execute tab with multiple active workflows shows a selectable list of active workflows"
    - "Execute tab icon in bottom bar shows a badge with the count of active workflows when count > 0"
    - "Overview tab is completely removed from navigation -- only 4 tabs remain (Home, Execute, History, Settings)"
    - "FormActionButtons.tsx file does not exist on disk"
    - "No remaining imports or exports reference FormActionButtons anywhere in the codebase"
  artifacts:
    - path: "apps/mobile/app/(tabs)/execute.tsx"
      provides: "Smart execute tab with empty state, single-workflow redirect, and multi-workflow list"
      contains: "useExecutionStore"
    - path: "apps/mobile/app/(tabs)/_layout.tsx"
      provides: "4-tab layout (Home, Execute, History, Settings) with active workflow badge"
      min_lines: 30
  key_links:
    - from: "apps/mobile/app/(tabs)/execute.tsx"
      to: "stores/execution-store.ts"
      via: "useExecutionStore selector for activeWorkflows"
      pattern: "useExecutionStore"
    - from: "apps/mobile/app/(tabs)/execute.tsx"
      to: "/execution/[instanceId]"
      via: "router.replace for single active workflow redirect"
      pattern: "router\\.replace"
---

<objective>
Replace placeholder Execute tab with smart active-workflow routing, remove Overview tab, and delete dead FormActionButtons component.

Purpose: Close three tech debt items from the v1.0 milestone audit -- the Execute tab stub, the Overview tab stub, and unused FormActionButtons. After this plan, the tab bar has 4 functional tabs with no placeholder stubs and no dead code from form components.

Output: Functional Execute tab with conditional rendering, cleaned-up 4-tab layout, and deleted dead code.
</objective>

<execution_context>
@C:\Users\dnbra\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\dnbra\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-ui-cleanup-dead-code/07-CONTEXT.md

Source files to modify:
@apps/mobile/app/(tabs)/execute.tsx
@apps/mobile/app/(tabs)/_layout.tsx
@apps/mobile/app/(tabs)/overview.tsx
@apps/mobile/src/components/form/FormActionButtons.tsx
@apps/mobile/src/components/form/index.ts
@apps/mobile/src/components/carousel/StepCarousel.tsx

Reference files (existing patterns):
@apps/mobile/src/stores/execution-store.ts
@apps/mobile/app/(tabs)/index.tsx
@apps/mobile/app/execution/[instanceId].tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace Execute tab and remove Overview tab</name>
  <files>
    apps/mobile/app/(tabs)/execute.tsx
    apps/mobile/app/(tabs)/_layout.tsx
    apps/mobile/app/(tabs)/overview.tsx
  </files>
  <action>
    **execute.tsx** -- Complete rewrite. Replace the placeholder stub with a smart routing screen:

    1. Import `useExecutionStore` from `../../src/stores/execution-store` and `useRouter` + `Href` from `expo-router`. Import `useFocusEffect` from `expo-router` to trigger redirect on tab focus (not just mount). Import `FontAwesome` from `@expo/vector-icons/FontAwesome` for the empty state icon.

    2. Read `activeWorkflows` from `useExecutionStore((s) => s.activeWorkflows)`. Derive `activeList = Object.values(activeWorkflows)` in a useMemo.

    3. Three-branch conditional rendering:
       - **Zero active workflows:** Show an empty state screen. Centered content with a FontAwesome `play-circle` icon (size 64, color `colors.textSecondary` with reduced opacity), heading "No Active Workflow", body text "Start a workflow from the Library tab on the Home screen". Use SafeAreaView with `edges={['bottom']}`, consistent styling with the Home screen empty states (same typography, spacing, colors from `@brainpal/ui`).
       - **Exactly one active workflow:** Use `useFocusEffect` to call `router.replace(`/execution/${activeList[0].instanceId}` as Href)` when the tab comes into focus and activeList.length === 1. Show a brief loading/redirecting state (ActivityIndicator centered) while the redirect happens, so the screen is not blank.
       - **Multiple active workflows:** Render a FlatList of active workflows. Each item shows the workflow name and a StateBadge (import from `../../src/components/workflow/WorkflowCard` -- check if StateBadge is exported separately, if not, create a simple inline badge). Tapping an item calls `router.push(`/execution/${item.instanceId}` as Href)`. Use the same card styling pattern as the Home screen's ActiveWorkflowCard. Include a header "Active Workflows" above the list.

    Important: Use `router.replace` (not `router.push`) for the single-workflow redirect so the user can press back to go to the previous tab, not back to a blank execute screen.

    **_layout.tsx** -- Modify the tab layout:

    1. Remove the entire `<Tabs.Screen name="overview" .../>` block (lines ~40-48).
    2. Tab order becomes: index (Home), execute (Execute), history (History), settings (Settings).
    3. Add a badge to the Execute tab. Import `useExecutionStore` from `../../src/stores/execution-store`. Read `activeCount = Object.keys(useExecutionStore((s) => s.activeWorkflows)).length`. On the execute tab's options, add `tabBarBadge: activeCount > 0 ? activeCount : undefined`. Style with `tabBarBadgeStyle: { backgroundColor: colors.primary }`.
    4. Add `headerShown: false` to the execute tab options (matching index tab pattern, since execute.tsx handles its own SafeAreaView).

    **overview.tsx** -- Delete the file entirely. Use the Bash tool: `rm apps/mobile/app/\(tabs\)/overview.tsx`
  </action>
  <verify>
    1. `npx tsc --noEmit` passes (no TypeScript errors from removed overview references or new execute.tsx)
    2. `grep -r "overview" apps/mobile/app/` returns zero results (no remaining references)
    3. `ls apps/mobile/app/\(tabs\)/` shows only: _layout.tsx, execute.tsx, history.tsx, index.tsx, settings.tsx (no overview.tsx)
    4. `grep "useExecutionStore" apps/mobile/app/\(tabs\)/execute.tsx` confirms store is wired
    5. `grep "router.replace" apps/mobile/app/\(tabs\)/execute.tsx` confirms redirect is wired
  </verify>
  <done>
    Execute tab renders three conditional states (empty, redirect, list). Overview tab deleted. Tab layout has 4 tabs with active workflow badge on Execute.
  </done>
</task>

<task type="auto">
  <name>Task 2: Delete FormActionButtons dead code</name>
  <files>
    apps/mobile/src/components/form/FormActionButtons.tsx
    apps/mobile/src/components/form/index.ts
    apps/mobile/src/components/carousel/StepCarousel.tsx
  </files>
  <action>
    1. Delete `apps/mobile/src/components/form/FormActionButtons.tsx` entirely using Bash: `rm apps/mobile/src/components/form/FormActionButtons.tsx`

    2. Edit `apps/mobile/src/components/form/index.ts` to remove lines 9-10:
       ```
       export { FormActionButtons } from './FormActionButtons';
       export type { FormActionButtonsProps } from './FormActionButtons';
       ```

    3. Edit `apps/mobile/src/components/carousel/StepCarousel.tsx` line 2 comment. Change:
       `// user interaction steps. Each page renders a FormCanvas + FormActionButtons.`
       to:
       `// user interaction steps. Each page renders a FormCanvas with embedded action buttons.`
       This is a comment-only change -- FormActionButtons was never actually imported in this file.

    4. Verify no other files import FormActionButtons: `grep -r "FormActionButtons" apps/ packages/` should return zero results after cleanup.
  </action>
  <verify>
    1. `ls apps/mobile/src/components/form/FormActionButtons.tsx` fails (file deleted)
    2. `grep -r "FormActionButtons" apps/ packages/` returns zero results
    3. `npx tsc --noEmit` passes (no dangling imports)
  </verify>
  <done>
    FormActionButtons.tsx deleted. Barrel export cleaned. No remaining references in codebase. StepCarousel comment updated to reflect actual implementation.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` -- full TypeScript compilation passes
2. `ls apps/mobile/app/\(tabs\)/` shows exactly 5 files: _layout.tsx, execute.tsx, history.tsx, index.tsx, settings.tsx
3. `grep -r "overview" apps/mobile/app/` -- zero results
4. `grep -r "FormActionButtons" apps/ packages/` -- zero results
5. `grep "tabBarBadge" apps/mobile/app/\(tabs\)/_layout.tsx` -- badge is configured on Execute tab
</verification>

<success_criteria>
- Execute tab shows empty state when no active workflows (not "Coming in Phase 3")
- Execute tab auto-redirects to execution screen when exactly one workflow is active
- Execute tab shows selectable list when multiple workflows are active
- Execute tab icon shows active workflow count badge
- Only 4 tabs visible: Home, Execute, History, Settings (Overview removed)
- FormActionButtons component fully deleted with zero residual references
- TypeScript compilation clean
</success_criteria>

<output>
After completion, create `.planning/phases/07-ui-cleanup-dead-code/07-01-SUMMARY.md`
</output>
