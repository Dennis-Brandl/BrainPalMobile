---
phase: 03-execution-ui
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - apps/mobile/app/(tabs)/index.tsx
  - apps/mobile/app/(tabs)/_layout.tsx
  - apps/mobile/app/execution/_layout.tsx
  - apps/mobile/app/execution/library/[oid].tsx
  - apps/mobile/src/components/workflow/WorkflowCard.tsx
  - apps/mobile/src/components/workflow/StateBadge.tsx
  - apps/mobile/src/hooks/useDeviceType.ts
autonomous: true

must_haves:
  truths:
    - "Home screen shows two tabs: Active (runtime workflows) and Library (downloaded master workflows)"
    - "Active tab displays runtime workflow rows with state badge, current step name, and step progress"
    - "Library tab displays master workflow rows with version, description, and step count"
    - "Tapping a library workflow opens a detail screen with Start Execution button"
    - "Tapping Start Execution creates a runtime workflow and navigates to execution screen"
    - "Tapping an active workflow navigates to execution screen for that workflow"
    - "Bottom tab navigation includes Home, Execute, Overview, History, Settings tabs"
  artifacts:
    - path: "apps/mobile/app/(tabs)/index.tsx"
      provides: "Home screen with Active/Library tabs"
      contains: "Active.*Library"
    - path: "apps/mobile/app/execution/library/[oid].tsx"
      provides: "Library workflow detail/preview screen"
      contains: "Start Execution"
    - path: "apps/mobile/src/components/workflow/StateBadge.tsx"
      provides: "Color-coded ISA-88 state badge component"
      exports: ["StateBadge"]
    - path: "apps/mobile/src/components/workflow/WorkflowCard.tsx"
      provides: "Reusable workflow row for both active and library lists"
      exports: ["ActiveWorkflowCard", "LibraryWorkflowCard"]
  key_links:
    - from: "apps/mobile/app/execution/library/[oid].tsx"
      to: "apps/mobile/src/providers/EngineProvider.tsx"
      via: "useEngine().runner.createWorkflow() to start execution"
      pattern: "runner\\.createWorkflow"
    - from: "apps/mobile/app/(tabs)/index.tsx"
      to: "apps/mobile/src/stores/execution-store.ts"
      via: "useExecutionStore for active workflow list"
      pattern: "useExecutionStore"
    - from: "apps/mobile/app/(tabs)/index.tsx"
      to: "apps/mobile/src/stores/workflow-store.ts"
      via: "useWorkflowStore for master workflow library"
      pattern: "useWorkflowStore"
---

<objective>
Build the Home screen with Active/Library tabs showing runtime and master workflows, a library detail/preview screen with "Start Execution" button, and navigation wiring from home to execution screens.

Purpose: Users need to see their downloaded workflows and active executions, launch new workflows, and navigate to running executions. This is the primary entry point for all workflow interaction.
Output: Fully functional Home screen with dual tabs, library detail screen, state badges, navigation routing, and workflow launch flow.
</objective>

<execution_context>
@C:\Users\dnbra\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\dnbra\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-execution-ui/03-CONTEXT.md
@.planning/phases/03-execution-ui/03-RESEARCH.md
@.planning/phases/03-execution-ui/03-01-SUMMARY.md

Key existing files to reference:
@apps/mobile/app/(tabs)/index.tsx (current home screen -- will be rewritten)
@apps/mobile/app/(tabs)/_layout.tsx (tab navigator -- may need execution stack integration)
@apps/mobile/app/_layout.tsx (root layout with provider nesting)
@apps/mobile/src/stores/workflow-store.ts (master workflow store + runtime workflow queries)
@apps/mobile/src/stores/execution-store.ts (active execution state from 03-01)
@apps/mobile/src/providers/EngineProvider.tsx (useEngine hook from 03-01)
@packages/engine/src/types/common.ts (WorkflowState, StepState for badge styling)
@packages/engine/src/types/master.ts (MasterWorkflowSpecification for library detail)
@packages/ui/src/theme/colors.ts (color palette)
@packages/ui/src/theme/typography.ts (font styles)
@packages/ui/src/theme/spacing.ts (spacing tokens)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared components and hooks</name>
  <files>
    apps/mobile/src/components/workflow/StateBadge.tsx
    apps/mobile/src/components/workflow/WorkflowCard.tsx
    apps/mobile/src/hooks/useDeviceType.ts
  </files>
  <action>
**1. Create StateBadge component** (`apps/mobile/src/components/workflow/StateBadge.tsx`):

A color-coded pill badge displaying ISA-88 workflow or step state text.

Props: `{ state: WorkflowState | StepState; size?: 'small' | 'default' }`

Color mapping (use colors from `@brainpal/ui`):
- `RUNNING`, `EXECUTING`, `STARTING`, `COMPLETING` -> colors.primary (blue) background with white text
- `PAUSED`, `PAUSING`, `UNPAUSING`, `HELD`, `HOLDING`, `UNHOLDING` -> colors.warning (amber) background with dark text
- `COMPLETED` -> colors.success (green) background with white text
- `ABORTED`, `ABORTING` -> colors.error (red) background with white text
- `STOPPED`, `STOPPING` -> colors.textSecondary (gray) background with white text
- `IDLE`, `WAITING` -> colors.border (light gray) background with dark text
- All other states -> default gray

Style: rounded pill (borderRadius: 12), horizontal padding, small text. `size: 'small'` uses caption typography; `size: 'default'` uses body typography.

Display the state name formatted for readability: replace underscores with spaces and use title case (e.g., "WAIT_ALL" -> "Wait All", "IN_PROGRESS" -> "In Progress").

**2. Create WorkflowCard components** (`apps/mobile/src/components/workflow/WorkflowCard.tsx`):

Two card variants exported from same file:

**ActiveWorkflowCard**: For runtime workflows on the Active tab.
Props: `{ workflow: RuntimeWorkflowSummary; onPress: () => void }`
Layout:
- Row with workflow name (left) + StateBadge (right)
- Second row: current step name (if available, from execution store) + step progress "Step N/M"
- Third row: time since started or time in current state (relative time like "2m ago", "1h ago")
- Pressable wrapper navigating to execution screen on tap
- Card style matching existing card pattern (surface bg, border, rounded corners)

**LibraryWorkflowCard**: For master workflows on the Library tab.
Props: `{ workflow: MasterWorkflow; onPress: () => void }`
Layout:
- Row with workflow name (local_id) + version badge (existing pattern)
- Description text (truncated to 2 lines)
- Step count at bottom ("12 steps")
- Pressable wrapper navigating to library detail screen

To get step count for library cards: parse the master workflow's specification_json to count steps. Since the workflow store's MasterWorkflow type doesn't include step count, add a `stepCount` field to MasterWorkflow (derived during loadFromDb by parsing specification_json and counting steps array length).

**3. Create useDeviceType hook** (`apps/mobile/src/hooks/useDeviceType.ts`):

```typescript
type DeviceType = 'phone' | 'tablet' | 'desktop';

function useDeviceType(): DeviceType {
  const { width } = useWindowDimensions();
  if (width >= 1024) return 'desktop';
  if (width >= 768) return 'tablet';
  return 'phone';
}
```

Uses React Native's `useWindowDimensions` -- no external dependencies needed.
  </action>
  <verify>
Run `npx tsc --noEmit` for zero type errors. Verify StateBadge renders all workflow states with correct colors. Verify WorkflowCard components accept the expected prop types.
  </verify>
  <done>
StateBadge renders color-coded pills for all ISA-88 states. WorkflowCard variants display active and library workflows with appropriate info. useDeviceType hook detects phone/tablet/desktop from screen width.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite Home screen with Active/Library tabs and create library detail screen</name>
  <files>
    apps/mobile/app/(tabs)/index.tsx
    apps/mobile/app/(tabs)/_layout.tsx
    apps/mobile/app/execution/_layout.tsx
    apps/mobile/app/execution/library/[oid].tsx
    apps/mobile/src/stores/workflow-store.ts
  </files>
  <action>
**1. Update workflow store for step count** (`apps/mobile/src/stores/workflow-store.ts`):

Add `stepCount: number` field to the `MasterWorkflow` interface. In `rowToWorkflow`, parse `specification_json` from the row to extract `steps.length`. This requires the store to access the full row data (the existing loadFromDb already queries all columns). Update `rowToWorkflow`:

```typescript
function rowToWorkflow(row: MasterWorkflowRow): MasterWorkflow {
  let stepCount = 0;
  try {
    const spec = JSON.parse(row.specification_json);
    stepCount = spec.steps?.length ?? 0;
  } catch { /* ignore parse errors */ }
  return {
    oid: row.oid,
    local_id: row.local_id,
    version: row.version,
    description: row.description,
    downloaded_at: row.downloaded_at,
    package_file_name: row.package_file_name,
    stepCount,
  };
}
```

Also add `getSpecificationJson: (oid: string) => Promise<string | null>` method that queries `SELECT specification_json FROM master_workflows WHERE oid = ?` -- needed by the library detail screen to get the full spec for creating runtime workflows.

**2. Rewrite Home screen** (`apps/mobile/app/(tabs)/index.tsx`):

Replace the entire current home screen with a dual-tab layout.

Structure:
- Top area: "BrainPal Mobile" title
- Tab bar: two tabs -- "Active" and "Library" -- implemented as a simple segmented control (two Pressable buttons with underline indicator on the active tab)
- Content area: FlatList showing either active or library workflows depending on selected tab

Active tab:
- Uses `useExecutionStore` to get active workflows (from `activeWorkflows` Record)
- Also uses `useWorkflowStore` to get `runtimeWorkflows` for display data
- Each row is an `ActiveWorkflowCard`
- `onPress` navigates to `/execution/${instanceId}` using `router.push()`
- Empty state: "No active workflows" with prompt to start one from Library

Library tab:
- Uses `useWorkflowStore` to get `masterWorkflows`
- Each row is a `LibraryWorkflowCard`
- `onPress` navigates to `/execution/library/${oid}` using `router.push()`
- Empty state: "No workflows downloaded. Import a .WFmasterX package to get started"

Import file picker for library tab: Add a floating action button (FAB) or header button on the Library tab that triggers file import. Use `expo-document-picker` to select a .WFmasterX file, then call the engine's PackageImporter. (If PackageImporter requires repository implementations that aren't wired yet, add a TODO comment -- the import flow was partially built in Phase 2 engine-side. For now, the button should exist but may show "Coming soon" if full wiring isn't ready. The primary path is that workflows are already imported and appear in the library.)

Actually -- the import flow is already handled: Phase 2 built PackageImporter in the engine. Phase 3 plan 01 creates the SQLite repositories. We need to wire the import: file picker -> read bytes -> PackageImporter.importPackage() -> repo.save(). Add a simple import button on the Library tab header that uses `DocumentPicker.getDocumentAsync()` to pick a file, reads it as a Uint8Array, and calls the engine's import pipeline. Use `useEngine()` from EngineProvider to access repos, then create a PackageImporter with the master repos.

**3. Create execution stack layout** (`apps/mobile/app/execution/_layout.tsx`):

Simple Stack navigator for execution-related screens:
```typescript
import { Stack } from 'expo-router';

export default function ExecutionLayout() {
  return (
    <Stack>
      <Stack.Screen name="[instanceId]" options={{ headerShown: false }} />
      <Stack.Screen name="library/[oid]" options={{ title: 'Workflow Details' }} />
    </Stack>
  );
}
```

**4. Create library detail screen** (`apps/mobile/app/execution/library/[oid].tsx`):

Route parameter: `oid` (master workflow OID).

Uses `useLocalSearchParams()` to get the OID, then:
1. Calls `useWorkflowStore().masterWorkflows` to find the workflow by OID
2. Displays:
   - Workflow name (local_id) as title
   - Version badge
   - Full description
   - Step count
   - Step type breakdown (e.g., "4 User Interaction, 2 Yes/No, 1 Parallel") -- parse specification_json to count by step_type
   - Downloaded date
3. "Start Execution" button at the bottom (prominent, primary color, full width)
4. On press:
   - Get full specification_json from workflow store (via getSpecificationJson method)
   - Parse it to MasterWorkflowSpecification
   - Call `useEngine().runner.createWorkflow(spec)` to create runtime workflow
   - Call `useEngine().runner.startWorkflow(instanceId)` to begin execution
   - Add to execution store: `useExecutionStore().addActiveWorkflow(instanceId, oid, name)`
   - Navigate to `/execution/${instanceId}` using `router.replace()`
5. Loading state while creating workflow

**5. Update tab layout** (`apps/mobile/app/(tabs)/_layout.tsx`):

The existing tab layout is fine. Ensure the execution route stack is accessible. In Expo Router, the `execution/` directory creates a nested stack automatically. Ensure `app/_layout.tsx` includes the execution Stack.Screen:

In root layout (`apps/mobile/app/_layout.tsx`), add:
```typescript
<Stack.Screen name="execution" options={{ headerShown: false }} />
```
alongside the existing `(tabs)` screen.
  </action>
  <verify>
Run `npx tsc --noEmit` for zero type errors. Start the app and verify:
1. Home screen shows Active/Library tab switcher
2. Library tab lists downloaded master workflows (if any exist from prior testing)
3. Tapping a library workflow navigates to detail screen showing workflow info
4. "Start Execution" button is visible on detail screen
5. Bottom tabs (Home, Execute, Overview, History, Settings) are all present
  </verify>
  <done>
Home screen displays dual Active/Library tabs with appropriate workflow cards. Library detail screen shows workflow information with Start Execution button. Navigation from home to execution screens is wired. File import button exists on Library tab.
  </done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with zero errors
2. Home screen renders with Active/Library tab switcher
3. Active tab shows runtime workflows with state badges (empty if none running)
4. Library tab shows master workflows with version, description, step count
5. Tapping library workflow -> detail screen with Start Execution button
6. Starting execution creates workflow + navigates to execution route
7. State badges use correct colors for all workflow states
8. Bottom tab navigation works for all 5 tabs
</verification>

<success_criteria>
- Home screen has functioning Active/Library tab navigation
- Library workflows display with name, version, description, step count
- Library detail screen allows launching workflow execution
- Active workflows display with state badges and progress info
- Navigation from Home to execution screens works correctly
- Bottom tab bar includes all 5 tabs
</success_criteria>

<output>
After completion, create `.planning/phases/03-execution-ui/03-02-SUMMARY.md`
</output>
